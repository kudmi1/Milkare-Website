import React from 'react'

export const AccordionText = ( {question, answer} ) => {
  return (
    <ul className='accordion-text'>
      {question.map((faqItem, id) => {
        
      })}
    </ul>
  )
}
